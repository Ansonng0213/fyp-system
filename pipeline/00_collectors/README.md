Collector scripts (run only when re-collecting fresh data, e.g. before submission).
API keys are ALREADY env-based: copy `.env.example` at repo root to `.env`,
fill in NEW keys (the old exposed ones must be revoked in the provider consoles),
then run from repo root. Requires: pip install python-dotenv (in requirements.txt).
