# App runtime — this is what Streamlit Community Cloud installs.
# Deliberately MINIMAL (only what app/ imports) AND pinned to the exact versions
# tested locally, so cloud == local and redeploys can't drift (e.g. a pydeck
# change that breaks tooltip HTML rendering). Full pipeline stack lives in
# requirements-pipeline.txt.
streamlit==1.59.2
pydeck==0.9.3
pandas==3.0.3
numpy==2.5.1
plotly==6.9.0
