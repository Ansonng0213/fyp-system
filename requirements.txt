# App runtime — this is what Streamlit Community Cloud installs.
# Deliberately MINIMAL: only what app/ imports, so the deploy is fast and never
# has to compile prophet / GDAL. The full pipeline stack is in
# requirements-pipeline.txt (not needed to run the dashboard).
streamlit>=1.36
pydeck>=0.9
pandas>=2.1
numpy>=1.26
plotly>=5.20
